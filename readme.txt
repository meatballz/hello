This is a note taking application written in javascript.

You can add a new note by typing this into the terminal:
node app.js add --title "new note" --body "new note body"

or you can read a note like this:
node app.js read --title "new note"

list all notes:
node app.js list

delete a note:
node app.js remove --title "new note"
